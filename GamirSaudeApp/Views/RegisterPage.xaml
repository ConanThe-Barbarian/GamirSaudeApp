<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:GamirSaudeApp.ViewModels"
             x:Class="GamirSaudeApp.Views.RegisterPage"
             x:DataType="viewmodels:RegisterViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="White">

    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="15">

            <Grid ColumnDefinitions="Auto, *" Margin="0,10,0,20">
                <ImageButton Grid.Column="0"
                             HeightRequest="20" WidthRequest="20"
                             HorizontalOptions="Start" VerticalOptions="Center"
                             Command="{Binding VoltarParaLoginCommand}">
                    <ImageButton.Source>
                        <FontImageSource Glyph="&lt;" Color="#0098DA" Size="25"/>
                    </ImageButton.Source>
                </ImageButton>

                <Label Grid.Column="1" Text="Cadastro" TextColor="#0098DA" FontSize="22" FontAttributes="Bold"
                       HorizontalOptions="Center" VerticalOptions="Center" Margin="-20,0,0,0"/>
            </Grid>

            <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" BackgroundColor="#EFF4FF" HeightRequest="50" Padding="15,0">
                <Entry Text="{Binding Nome}" Placeholder="nome completo" PlaceholderColor="#8EA6CC" TextColor="#0098DA" VerticalOptions="Center"/>
            </Border>

            <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" BackgroundColor="#EFF4FF" HeightRequest="50" Padding="15,0">
                <Entry Text="{Binding Cpf}" Placeholder="cpf" PlaceholderColor="#8EA6CC" TextColor="#0098DA" Keyboard="Numeric" VerticalOptions="Center"/>
            </Border>

            <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" BackgroundColor="#EFF4FF" HeightRequest="50" Padding="15,0">
                <Entry Text="{Binding Email}" Placeholder="Email" PlaceholderColor="#8EA6CC" TextColor="#0098DA" Keyboard="Email" VerticalOptions="Center"/>
            </Border>

            <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" BackgroundColor="#EFF4FF" HeightRequest="50" Padding="15,0">
                <Entry Text="{Binding Celular}" Placeholder="celular" PlaceholderColor="#8EA6CC" TextColor="#0098DA" Keyboard="Telephone" VerticalOptions="Center"/>
            </Border>

            <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" BackgroundColor="#EFF4FF" HeightRequest="50" Padding="15,0">
                <Picker Title="sexo" TitleColor="#8EA6CC" TextColor="#0098DA"
                        ItemsSource="{Binding ListaSexos}" SelectedItem="{Binding SexoSelecionado}" VerticalOptions="Center"/>
            </Border>

            <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" BackgroundColor="#EFF4FF" HeightRequest="50" Padding="15,0">
                <DatePicker Date="{Binding DataNascimento}" TextColor="#0098DA" Format="dd/MM/yyyy" VerticalOptions="Center"/>
            </Border>

            <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" BackgroundColor="#EFF4FF" HeightRequest="50" Padding="15,0">
                <Entry Text="{Binding Senha}" Placeholder="senha" PlaceholderColor="#8EA6CC" TextColor="#0098DA" IsPassword="True" VerticalOptions="Center"/>
            </Border>

            <Border StrokeShape="RoundRectangle 10" StrokeThickness="0" BackgroundColor="#EFF4FF" HeightRequest="50" Padding="15,0">
                <Entry Text="{Binding ConfirmarSenha}" Placeholder="confirmar senha" PlaceholderColor="#8EA6CC" TextColor="#0098DA" IsPassword="True" VerticalOptions="Center"/>
            </Border>

            <BoxView HeightRequest="10" Color="Transparent"/>

            <Button Text="Cadastrar" Command="{Binding CadastrarCommand}" BackgroundColor="#0098DA" TextColor="White" FontAttributes="Bold" CornerRadius="25" HeightRequest="50" IsEnabled="{Binding IsNotBusy}"/>

            <Label Text="Ja possui uma conta no App Gamir?" TextColor="#666666" FontSize="12" HorizontalOptions="Center" Margin="0,5,0,0"/>

            <Button Text="Login" Command="{Binding VoltarParaLoginCommand}" BackgroundColor="#D6E4FF" TextColor="#0098DA" FontAttributes="Bold" CornerRadius="25" HeightRequest="50" IsEnabled="{Binding IsNotBusy}"/>

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#0098DA" HorizontalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>